.row-fluid
  .span4
    %h3 Welcome to GoGetix
    = link_to 'Print tickets', new_user_session_path, :class => 'btn btn-primary'
    %h4 or Organize your events on the best way
  .span4
    = render 'devise/sessions/new'
  .span4
    %h4 Sign up as Organizer
    = render :partial => 'devise/registrations/new', :locals => {:role => :organizer}