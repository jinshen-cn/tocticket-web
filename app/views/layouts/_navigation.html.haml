= link_to image_tag("logo.png"), root_path, :class => 'brand'
#main-nav.bottom
  %ul.nav.nav-pills
    - if user_signed_in?
      %li= nav_link 'My Events', events_path if current_user.has_role? :organizer
      %li= nav_link 'My Tickets', my_tickets_path if current_user.has_role? :attendee
      %li= link_to 'Logout', destroy_user_session_path, :method=>'delete'
      %li= nav_link 'Edit account', edit_user_registration_path