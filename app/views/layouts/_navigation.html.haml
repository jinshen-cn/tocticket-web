= link_to image_tag("logo.png"), root_path, :class => 'brand'
#main-nav.bottom
  %ul.nav.nav-pills
    - if user_signed_in?
      %li= nav_link t('layouts.navigation.my_events'), events_path if current_user.has_role? :organizer
      %li= nav_link t('layouts.navigation.my_tickets'), my_tickets_path if current_user.has_role? :attendee
      %li= link_to t('layouts.navigation.logout'), destroy_user_session_path, :method=>'delete'
      %li= nav_link t('layouts.navigation.edit_account'), edit_user_registration_path